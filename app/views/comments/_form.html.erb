<div class="comment-area materialize-textarea">
  <%= simple_form_for([@post, @post.comments.build]) do |f| %>
    <%= f.input :content, label: "Add Comment", class: "label comment-label" %>
    <br>
    <%= f.button :submit, class: "button comment-btn" %>
  <% end %>  
</div>
